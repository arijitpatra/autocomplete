export const ALL_COUNTRIES_ENDPOINT = "https://restcountries.com/v3.1/all";

// export const getNameSearchEndpoint = (searchText: string) =>
//   `${BASE_ENDPOINT}/name/${searchText}?fields=name,flags`;
